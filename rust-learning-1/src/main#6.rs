/*
loop를 사용하여 영원히 반복
loop 식은 무한 루프를 나타냅니다. 계속해서 본문의 실행을 반복합니다.
*/

fn main () {
    /*
    loop {
        println!("I loop forever");
    }   
    */

    //Rust에 있는 다른 종류의 루프(예: While 및 for)와 달리 break를 통해 값을 반환하는 식에 loop를 사용할 수 있습니다.
    /*
    let mut i = 1;
    let something = loop {
        i *= 2;
        if i > 100 {
            break i;
        }
    };
    assert_eq!(something, 128);
    println!("{:#?}", something);
    */

}